- name: Ensure SBM Power-User exists (vendor sample + wrapper)
  hosts: sbms
  gather_facts: false
  vars:
    ansible_become: true
  roles:
    - role: sbm_api_samples
      tasks_from: verify_samples.yml
  tasks:
    - include_role:
        name: sbm_api_samples
        tasks_from: ensure_power_user.yml
